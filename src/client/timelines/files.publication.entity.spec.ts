import { FilesPublicationEntity } from './files.publication.entity';

describe('FilesPublication.Entity', () => {
  it('should be defined', () => {
    expect(new FilesPublication.Entity()).toBeDefined();
  });
});
